plugins {
    id "org.jetbrains.kotlin.multiplatform"
    id "org.jetbrains.kotlin.plugin.serialization"
}

apply from: "$mppJvmJs"

kotlin {
    sourceSets {
        commonMain {
            dependencies {
                api project(":frameswork.features.common.common")
                api project(":frameswork.features.frames.common")
            }
        }
    }
}
