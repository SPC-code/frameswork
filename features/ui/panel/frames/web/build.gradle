plugins {
    id "org.jetbrains.kotlin.multiplatform"
    id "org.jetbrains.kotlin.plugin.serialization"
    id "com.google.devtools.ksp"
    alias(libs.plugins.compose)
    alias(libs.plugins.kt.compose)
}

apply from: "$mppJsWithCompose"

kotlin {
    sourceSets {
        commonMain {
            dependencies {
                api project(":frameswork.features.ui.panel.frames.common")
                api project(":frameswork.features.ui.panel")
                api project(":frameswork.features.frames.web")
            }
        }
    }
}

enableKspForCommon(this)
enableKspForJs(this)
