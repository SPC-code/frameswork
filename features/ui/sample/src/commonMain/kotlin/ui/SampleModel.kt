package space.kscience.frameswork.features.ui.sample.ui

/**
 * Model contract consumed by [SampleViewModel].
 *
 * The sample plugin supplies an empty singleton implementation so the view-model wiring has an
 * explicit model dependency even though the sample currently exposes no operations.
 */
interface SampleModel {

}
